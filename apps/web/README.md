# Web demo

This directory is the shareable browser experience. The pilot is intentionally
dependency-free so stakeholders can run it without provider keys:

```powershell
python -m http.server 4175 -d apps/web
```
