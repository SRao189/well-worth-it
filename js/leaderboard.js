const navLinks=document.querySelectorAll('.bottom-nav a'); const screens=document.querySelectorAll('.screen'); function show(id){const valid=[...screens].some(s=>s.id===id)?id:'main-game';screens.forEach(s=>s.classList.toggle('is-active',s.id===valid));navLinks.forEach(a=>{const active=a.getAttribute('href')==='#'+valid;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});window.scrollTo({top:0,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})}window.addEventListener('hashchange',()=>show(location.hash.slice(1)||'main-game'));show(location.hash.slice(1)||'main-game');document.querySelector('#claimReward').addEventListener('click',e=>{e.currentTarget.textContent='Reward claimed locally';e.currentTarget.disabled=true});
